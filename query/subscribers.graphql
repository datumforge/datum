query Subscribers($where: SubscriberWhereInput) {
  subscribers(where: $where) {
    edges {
      node {
        id
        email
        verifiedEmail
        active
        ownerID
      }
    }
  }
}